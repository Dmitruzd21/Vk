package manager;

import domain.Post;

public class PostManager {
    private Post[] Posts;

    private int ownerId;
    private String domain;
    private String query;
    private boolean ownersOnly;
    private int count;
    private int offset;

    public Post[] WallSearch (int ownerId,String domain,String query,boolean ownersOnly, int count, int offset) {
        for (Post:Posts) {

            //Логика
        }
        return null;
    }

    public void WallDelete(int ownerId,String domain,String query,boolean ownersOnly, int count, int offset) {
        for (Post:Posts) {

            //Логика}
        }

    }
!!
}
